# 1、These requirements include all dependencies for all top-level python scripts for llama.cpp. 
#   从计算机系统的角度来看，"requirements"在不同上下文中有着略有不同的翻译和解释，具体取决于其在硬件兼容性还是软件依赖性方面的应用。
# 从当前的`requirements.txt`文件的上下文中理解，requirement应该被理解为依赖需求（从软件的角度来讲应该被翻译为依赖需求）。
#
# 2、Avoid adding packages here directly.
# 这句提示开发者不要直接在这个文件中添加新的依赖包，可能是为了保持依赖管理的清晰和有序。
#
# 3、Package versions must stay compatible across all top-level python scripts.
# 强调所有列出的依赖包版本必须保持兼容性，确保不同脚本之间的依赖版本不会冲突，保持整个项目的稳定性。


# 这些行使用了`-r`标志，它是pip安装命令的一部分，用于包含其他需求文件。这种方式通常用于大型项目中，把不同部分或模块的依赖分开管理，以保持整洁和易于管理。

# 1、包含将旧版`llama`数据或代码转换为新格式的依赖。
-r ./requirements/requirements-convert_legacy_llama.txt

# 2、包含将`hugging face`数据转换为gguf新格式的依赖。
-r ./requirements/requirements-convert_hf_to_gguf.txt
# 3、包含将`hugging face`数据转换为gguf新格式的依赖，更新后的版本。
-r ./requirements/requirements-convert_hf_to_gguf_update.txt
# 4、包含将`ggml`数据转换为gguf新格式的依赖。
-r ./requirements/requirements-convert_llama_ggml_to_gguf.txt
# 5、包含将`lora`数据转换为gguf新格式的依赖。
-r ./requirements/requirements-convert_lora_to_gguf.txt
